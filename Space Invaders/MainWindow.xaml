<Window x:Class="Space_Invaders.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Space_Invaders"
        mc:Ignorable="d"
         WindowStartupLocation="CenterScreen"

        Icon="C:\Users\danii\Desktop\Space Invaders_original\Space Invaders\Images\LOGO-removebg-preview.png"
        Title="Space Invaders" Height="500" Width="800">
    <Window.Background>
        <ImageBrush ImageSource="Images/Back_Space.png"/>
    </Window.Background>
    <Window.Resources>
        <!-- Стиль кнопок у стилі Space Invaders -->
        <Style x:Key="SpaceInvadersButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="200"/>
            <Setter Property="Height" Value="60"/>
            <Setter Property="Margin" Value="0,10,0,10"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#00FF00"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="#00FF00"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="MainBorder" 
                                    Background="Black" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" 
                                    CornerRadius="5">
                                <Border x:Name="GlowBorder" 
                                        BorderBrush="#004000" 
                                        BorderThickness="1" 
                                        CornerRadius="3.5" 
                                        Margin="1">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </Border>
                            <!-- Pixelated Invader Icon (pixel-art design using Path) -->
                            <Path x:Name="InvaderIcon" 
                                  Data="M3,0 L5,0 L5,1 L6,1 L6,2 L7,2 L7,3 L8,3 L8,5 L7,5 L7,6 L6,6 L6,7 L5,7 L5,8 L3,8 L3,7 L2,7 L2,6 L1,6 L1,5 L0,5 L0,3 L1,3 L1,2 L2,2 L2,1 L3,1 L3,0" 
                                  Fill="#00FF00" 
                                  Stretch="Uniform" 
                                  Width="20" 
                                  Height="20" 
                                  HorizontalAlignment="Right" 
                                  VerticalAlignment="Bottom" 
                                  Margin="0,0,10,10"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#00FFAA"/>
                                <Setter Property="Foreground" Value="#00FFAA"/>
                                <Setter TargetName="InvaderIcon" Property="Fill" Value="#00FFAA"/>
                                <Setter TargetName="GlowBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FF00" ShadowDepth="0" BlurRadius="15" Opacity="0.7"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="BorderBrush" Value="#FFFFFF"/>
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                                <Setter TargetName="InvaderIcon" Property="Fill" Value="#FFFFFF"/>
                                <Setter TargetName="GlowBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#00FF00" ShadowDepth="0" BlurRadius="5" Opacity="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Name="MyGrid" Focusable="True" FocusVisualStyle="{x:Null}">
        <StackPanel x:Name="MainMenuPanel" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="ГРАТИ" Style="{StaticResource SpaceInvadersButtonStyle}" Click="PlayButton_Click"/>
            <Button Content="НАЛАШТУВАННЯ" Style="{StaticResource SpaceInvadersButtonStyle}" Click="Button_Click"/>
        </StackPanel>
        <StackPanel x:Name="ModeSelectionPanel" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Button Content="ПЕРШИЙ РЕЖИМ" Style="{StaticResource SpaceInvadersButtonStyle}" Click="EasyModeButton_Click" />
            <Button Content="ДРУГИЙ РЕЖИМ" Style="{StaticResource SpaceInvadersButtonStyle}" Click="HardModeButton_Click" />
        </StackPanel>
        </Grid>
</Window>